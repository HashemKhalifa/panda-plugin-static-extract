# panda-plugin-static-extract

Auto-generate Panda CSS `staticCss` values by scanning component-boundary usage (like wrappers using `.raw()`).

## Status

Scaffold in progress.

## Planned Commands

- `panda-static-extract scan`
- `panda-static-extract audit`
- `panda-static-extract watch`
